import React  from "react";
import Home from "@/components/landingpage";
export default function FrontPage() {
    return (
        <main className="bg-green-400 min-h-screen flex items-center justify-center">
            <Home />
        </main>
    );
}